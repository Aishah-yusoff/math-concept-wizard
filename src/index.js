import React from "react";
import ReactDOM from "react-dom/client";
import MathQuestionBot from "./MathQuestionBot";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <MathQuestionBot />
  </React.StrictMode>
);